<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>rapidcode_passthrough_trajectory_controller</name>
  <version>0.1.0</version>
  <description>
    ros2_control passthrough trajectory controller for RapidCode. Hosts a
    FollowJointTrajectory action server and streams trajectory points to the
    hardware over the trajectory_transfer interfaces (the hardware interpolates
    via MovePVT), as an alternative to the stock JointTrajectoryController's
    per-cycle sampling.
  </description>
  <maintainer email="tech@roboticsys.com">RSI</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>controller_interface</depend>
  <depend>hardware_interface</depend>
  <depend>rclcpp</depend>
  <depend>rclcpp_action</depend>
  <depend>rclcpp_lifecycle</depend>
  <depend>pluginlib</depend>
  <depend>control_msgs</depend>
  <depend>trajectory_msgs</depend>
  <depend>realtime_tools</depend>
  <depend>std_srvs</depend>
  <depend>rapidcode_trajectory_transfer</depend>

  <test_depend>ament_cmake_gtest</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
