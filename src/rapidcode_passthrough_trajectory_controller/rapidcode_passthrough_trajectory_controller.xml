<library path="rapidcode_passthrough_trajectory_controller">
  <class
    name="rapidcode_passthrough_trajectory_controller/PassthroughTrajectoryController"
    type="rapidcode_passthrough_trajectory_controller::PassthroughTrajectoryController"
    base_class_type="controller_interface::ControllerInterface">
    <description>
      Passthrough trajectory controller: hosts a FollowJointTrajectory action
      server and streams the trajectory's points to hardware over the
      trajectory_transfer command/state interfaces (the hardware interpolates),
      rather than sampling one setpoint per cycle. Mutually exclusive with the
      stock joint_trajectory_controller.
    </description>
  </class>
</library>
